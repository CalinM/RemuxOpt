<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- Version that the workflow will read -->
    <Version>1.0.0</Version>

    <!-- Optional: Additional version-related properties -->
    <AssemblyVersion>1.0.0.15</AssemblyVersion>
    <FileVersion>1.0.0.15</FileVersion>

    <!-- Optional: Application metadata -->
    <AssemblyTitle>RemuxOpt</AssemblyTitle>
    <AssemblyDescription>Smart and flexible MVK remuxer</AssemblyDescription>
    <AssemblyCompany>CalinM</AssemblyCompany>
    <AssemblyProduct>RemuxOpt</AssemblyProduct>
    <Copyright>Copyright © CalinM 2025</Copyright>
  </PropertyGroup>

  <!-- Your dependencies here -->
  <ItemGroup>
    <!-- Example packages - replace with your actual dependencies -->
    <!-- <PackageReference Include="SomePackage" Version="1.0.0" /> -->
  </ItemGroup>

</Project>